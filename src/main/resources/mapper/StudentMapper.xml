<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dandan.wendy.community.mapper.StudentMapper">

	<!--查询所有学生-->
    <select id="findAll"  resultType="Student">
 		select * from student
 	</select>


	<!--保存新的学生-->
	<insert id="add" parameterType="Student">
        insert into student(name,sex,age,home,education,tutor,major,school)values(#{name},#{sex},#{age},#{home},#{education},#{tutor},#{major},#{school});
    </insert>

	<!--保存新的用户-->
	<!--<insert id="insert" parameterType="User">
        insert into user(name,accountId,token,gmtCreate,gmtModified,avatarUrl)
        values (#{name},#{accountId},#{token},#{gmtCreate},#{gmtModified},#{avatarUrl});
    </insert>-->
</mapper>